#include <stdio.h>

int main() {
    int num;
    int sum = 0;
    double average = 0;

    do {
        printf("Enter a number (0 to stop): ");
        scanf("%d", &num);
        
        
    } while (num != 0);

    return 0;
}